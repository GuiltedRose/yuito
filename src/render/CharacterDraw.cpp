#include "render/CharacterDraw.h"
#include <iostream>

void drawCharacterMesh(const Vec3& pos, const Quaternion& rot) {
    // Placeholder — connect to render system later
    std::cout << "Draw character at (" << pos.x << ", " << pos.y << ", " << pos.z << ")\n";
}
